+ Session Summary (2025-10-13)

Work Completed
- Added `--system-root` CLI flag and corresponding loader to run scripts headless against a real Frontier.root database.
- Expanded the headless build to link database/file modules and glue in the portable stubs needed to open `.root` files without UI dependencies.
- Implemented resilient load logic: missing optional tables (e.g., system.misc, system.menus) now trigger warnings and are hydrated in-memory so scripting can continue.
- Introduced CLI logging tweaks (warn level visible by default) and documentation updates covering the flag, usage, and current limitations.
- Drafted a headless hydration helper (read-only) that prepped the sanitized Frontier.root, though full on-disk migration is still blocked by legacy save code.
- Logged the outstanding need to modernize the database write/migration path in `planning/phase3/frontier_root_headless_plan.md`.

Next Steps
- Build a writable migration path (update tablesavesystemtable or add new tooling) so hydrated roots can be saved back as v7+.
- Once saving works, re-run `--hydrate-system-root` to patch the sanitized Frontier.root and add regression tests for loading both original and hydrated versions.
- Add an integration test that invokes `frontier-cli --system-root` to ensure future changes keep the headless load path intact.
- Follow with the planned script export work (system verb glue) once the database path is solid.
