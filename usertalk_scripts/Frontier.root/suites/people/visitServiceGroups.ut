on visitServiceGroups (serviceName, adrCallback) {
	on callback (groupName) {
		if people.serviceIsAttachedToGroup (serviceName, groupName) {
			try {
				adrCallback^ (groupName)}}};
	people.visitAllGroups (@callback);
	return (true)}