local (adrtable);if Frontier.tools.nodetypes.findnodetype ("", @adrtable) {	local (adrscript = @adrtable^.getInfo);	if defined (adrscript^) {		return (adrscript^ ())}	else {		return (false)}};return (false)