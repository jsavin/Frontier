local (adrtable);
if Frontier.tools.nodetypes.findnodetype ("", @adrtable) {
	local (adrscript = @adrtable^.collapse);
	if defined (adrscript^) {
		return (adrscript^ ())}
	else {
		return (false)}};
return (false)