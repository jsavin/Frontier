on normbuds (buddies) {	local {		buddy;		retStr=""};	for buddy in buddies {		retStr = retStr + " " + tcp.im.builtinDrivers.aim.code.util.normalize(buddy)};	return(string.popLeading(retStr, ' '))}