on getRandomPassword (numChars) {
	«Generate a random password numChars long.
		«Mon, Apr 27, 1998 at 11:00:10 AM by PBS
	local (password, i);
	for i = 1 to numChars {
		local (ch = random (33, 122));
		password = password + char (ch)};
	return (password)}