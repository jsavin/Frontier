on inResponder (adrPageTable) {	«Are we running from within the website framework responder?	if string.lower (html.getPref ("responder", adrPageTable)) == "websiteframework" {		return (true)};	return (false)}