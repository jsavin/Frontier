on keycodes ()
	//Enter the linenoise key-code debug mode.
		//5/6/26 by JES
			//Initial implementation. Wraps repl.printKeyCodes, which dispatches
			//to linenoisePrintKeyCodes() in the host.
	repl.printKeyCodes ()
	return (true)