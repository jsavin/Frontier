«Changes:
	«2/17/02; 2:05:18 AM by JS
		«Created. Set the template for a comments popup-browser-window.
local (pta = html.getPageTableAddress ());
local (adrsite = manilaSuite.getSiteAddress (pta));
local (adrtrackback = manilaSuite.trackback.init (adrsite));
local (adrtemplate = @adrtrackback^.templates.template);
pta^.template = string (adrtemplate^)