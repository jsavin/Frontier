«05/14/00; 11:37:29 PM by JES -- the localized version of the Flip Home Page page

local (htmltext, indentlevel = 0, pta = html.getPageTableAddress ());

if not (manilaSuite.editors.managingEditorTest (pta)) { //only a managing editor can flip the home page
	scriptError (manilaSuite.getString ("admin.cantFlipHomepageNotME"))}; // 4/18/00 JES: localized

pta^.title = manilaSuite.getString ("admin.flipHomePageTitle");

on add (s) {
	htmltext = htmltext + string.filledString ('\t', indentlevel) + s + "\r"};

add (manilaSuite.getString ("admin.flipHomePageBody"));

add ("<p>");
add ("<form method=\"POST\" action=\"postFlip\">"); indentlevel++;
add ("<input type=\"submit\" value=\"" + manilaSuite.getString ("admin.flipHomePageButton") + "\">");
add ("</form>"); indentlevel--;

return (htmltext)