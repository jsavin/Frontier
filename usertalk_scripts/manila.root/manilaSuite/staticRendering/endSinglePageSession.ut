on endSinglePageSession (adrSite) {
	«End a rendering session -- if this is a single page session.
		«03/14/00; 4:39:51 PM by PBS
	
	local (adrSiteInfo = manilaSuite.staticRendering.getStatusAddress (adrSite));
	
	if not (adrSiteInfo^.flWholeSite) {
		delete (adrSiteInfo)};
	
	return (true)}