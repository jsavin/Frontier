on authenticateMember (siteName, username, password) {
	«Return true if username is a member and the password is correct. Return false otherwise.
		«01/15/00; 6:32:09 PM by PBS
	
	local (groupName = manilaSuite.rpcUtils.getMembersGroupName (siteName));
	
	try { //try to authenticate the member using mainResponder's routine
		mainResponder.members.getMemberTableWithPassword (groupName, username, password);
		return (true)};
	
	return (false)}