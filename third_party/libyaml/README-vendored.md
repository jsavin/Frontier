# Vendored libyaml

This directory contains libyaml 0.2.5, retrieved from https://github.com/yaml/libyaml (MIT license).

We vendor the sources so the Frontier build/test pipeline can parse YAML without system dependencies.
Any updates should re-run the upstream test suite and refresh this note with the new version tag/date.
