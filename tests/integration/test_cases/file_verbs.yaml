# Integration tests for file verb operations
# Tests basic file operations available in headless mode
#
# Phase 1 priority file verbs (portable operations):
#   - file.fileFromPath() - Extract filename from path (returns string)
#   - file.exists() - Check if file exists
#   - file.new() - Create new file
#   - file.writeWholeFile() - Write data to file
#   - file.readWholeFile() - Read entire file
#   - file.delete() - Delete file
#
# NOTE: Many file verbs (metadata, resource fork, FSRef-specific) are Mac-only
# and not available in headless mode. Tests focus on portable POSIX operations.

tests:
  # Basic file existence checks
  # NOTE: file.fileFromPath() extracts the filename component from a path string
  # and returns it as a STRING, not a filespec. This is by design - it's a path
  # manipulation verb, not a type conversion verb. Therefore typeof() correctly
  # returns 'TEXT' (stringType), not 'fss ' (filespecType).
  - name: "file.fileFromPath - extract filename from path"
    description: "Extract filename component from path string (like basename)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fullPath = tmpDir + "/" + "test.txt");
      local(filename = file.fileFromPath(fullPath));
      return filename
    expected_success: true
    expected_result: "test.txt"

  - name: "file.exists - nonexistent file"
    description: "Check that nonexistent file returns false"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "nonexistent_file_12345.txt");
      return file.exists(fs)
    expected_success: true
    expected_result: "false"

  # File creation and writing
  - name: "file.new - create new empty file"
    description: "Create new file in project tmp directory"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "frontier_test_new.txt");
      local(ok = file.new(filePath));
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.exists - file created with file.new"
    description: "Verify file.new created the file"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_new.txt");
      return file.exists(fs)
    expected_success: true
    expected_result: "true"

  - name: "file.writeWholeFile - write string to file"
    description: "Write text content to file"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_write.txt");
      local(ok = file.writeWholeFile(fs, "Hello, Frontier!"));
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.readWholeFile - read back written content"
    description: "Read file and verify content matches"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_write.txt");
      local(readData = file.readWholeFile(fs));
      return readData
    expected_success: true
    expected_result: "Hello, Frontier!"

  - name: "file.readWholeFile - round trip data"
    description: |
      Write and read back data. readWholeFile returns 'data' type,
      so coerce to string for comparison.
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_roundtrip.dat");
      local(testData = "some test content 12345");
      file.writeWholeFile(fs, testData);
      local(readData = file.readWholeFile(fs));
      return (string(readData) == testData)
    expected_success: true
    expected_result: "true"

  # File deletion
  - name: "file.delete - delete existing file"
    description: "Delete file created in previous test"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_write.txt");
      local(ok = file.delete(fs));
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.exists - file deleted successfully"
    description: "Verify file.delete removed the file"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_write.txt");
      return file.exists(fs)
    expected_success: true
    expected_result: "false"

  # Error cases
  - name: "file.readWholeFile - nonexistent file error"
    description: "Reading nonexistent file should fail"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "does_not_exist_12345.txt");
      return file.readWholeFile(fs)
    expected_success: false
    expected_result: null

  - name: "file.delete - nonexistent file error"
    description: "Deleting nonexistent file should fail"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "does_not_exist_12345.txt");
      return file.delete(fs)
    expected_success: false
    expected_result: null

  # Metadata operations
  - name: "file.size - get file size"
    description: "Get size of file in bytes"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_size.txt");
      local(testData = "12345");
      file.writeWholeFile(fs, testData);
      return file.size(fs)
    expected_success: true
    expected_result: "5"

  - name: "file.created - get file creation date"
    description: "Get creation date of file (Frontier epoch)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_created.txt");
      file.new(fs);
      local(createdDate = file.created(fs));
      return typeof(createdDate)
    expected_success: true
    expected_result: "date"

  - name: "file.modified - get file modification date"
    description: "Get modification date of file (Frontier epoch)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_modified.txt");
      file.writeWholeFile(fs, "initial content");
      local(modifiedDate = file.modified(fs));
      return typeof(modifiedDate)
    expected_success: true
    expected_result: "date"

  # File operations
  - name: "file.copy - copy file to new location"
    description: "Copy file and verify both exist"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(source = tmpDir + "/" + "frontier_test_copy_source.txt");
      local(dest = tmpDir + "/" + "frontier_test_copy_dest.txt");
      file.writeWholeFile(source, "test content for copy");
      local(ok = file.copy(source, dest));
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.copy - verify destination exists with same content"
    description: "Check copied file has same content as source"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(source = tmpDir + "/" + "frontier_test_copy_source.txt");
      local(dest = tmpDir + "/" + "frontier_test_copy_dest.txt");
      local(sourceContent = file.readWholeFile(source));
      local(destContent = file.readWholeFile(dest));
      return (sourceContent == destContent)
    expected_success: true
    expected_result: "true"

  - name: "file.rename - rename file"
    description: "Rename file and verify new name exists"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(oldName = tmpDir + "/" + "frontier_test_rename_old.txt");
      local(newName = tmpDir + "/" + "frontier_test_rename_new.txt");
      file.writeWholeFile(oldName, "rename test content");
      local(ok = file.rename(oldName, newName));
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.rename - verify old name no longer exists"
    description: "Check that old filename is gone after rename"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(oldName = tmpDir + "/" + "frontier_test_rename_old.txt");
      return file.exists(oldName)
    expected_success: true
    expected_result: "false"

  - name: "file.rename - verify new name exists with same content"
    description: "Check renamed file has original content"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(newName = tmpDir + "/" + "frontier_test_rename_new.txt");
      local(content = file.readWholeFile(newName));
      return (string(content) == "rename test content")
    expected_success: true
    expected_result: "true"

  - name: "file.move - move file to different location"
    description: "Move file and verify it appears at destination"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(source = tmpDir + "/" + "frontier_test_move_source.txt");
      local(dest = tmpDir + "/" + "frontier_test_move_dest.txt");
      file.writeWholeFile(source, "move test content");
      local(ok = file.move(source, dest));
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.move - verify source no longer exists"
    description: "Check that source file is gone after move"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(source = tmpDir + "/" + "frontier_test_move_source.txt");
      return file.exists(source)
    expected_success: true
    expected_result: "false"

  - name: "file.move - verify destination exists with same content"
    description: "Check moved file has original content"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(dest = tmpDir + "/" + "frontier_test_move_dest.txt");
      local(content = file.readWholeFile(dest));
      return (string(content) == "move test content")
    expected_success: true
    expected_result: "true"

  - name: "file.newfolder - create new folder"
    description: "Create new folder in temp directory"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(folderPath = tmpDir + "/" + "frontier_test_newfolder_unique");
      if file.exists(folderPath) { file.delete(folderPath); };
      local(ok = file.newfolder(folderPath));
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.newfolder - verify folder exists"
    description: "Check that created folder exists"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(folderPath = tmpDir + "/" + "frontier_test_newfolder_unique");
      return file.exists(folderPath)
    expected_success: true
    expected_result: "true"

  # Path operations
  - name: "file.getpathchar - get path separator character"
    description: "Get platform-specific path separator (/ on Unix)"
    script: |
      local(pathChar = file.getpathchar());
      return pathChar
    expected_success: true
    expected_result: "/"

  - name: "file.fullpath - get absolute path"
    description: "Convert relative or partial path to absolute path"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_fullpath.txt");
      file.new(fs);
      local(fullPath = file.fullpath(fs));
      return (string.length(fullPath) > 0)
    expected_success: true
    expected_result: "true"

  - name: "file.fullpath - verify absolute path starts with /"
    description: "Absolute paths on Unix should start with /"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_fullpath.txt");
      local(fullPath = file.fullpath(fs));
      return (string.mid(fullPath, 1, 1) == "/")
    expected_success: true
    expected_result: "true"

  - name: "file.folderfrompath - extract folder from path"
    description: "Get folder portion of a file path (includes trailing slash per Mac behavior)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(testPath = tmpDir + "/" + "testfile.txt");
      local(folder = file.folderfrompath(testPath));
      return (folder == tmpDir + "/")
    expected_success: true
    expected_result: "true"

  - name: "file.getpath - get current working directory"
    description: "Get the current working directory path"
    skip: true
    skip_reason: "file.getpath() not implemented - CWD should be thread-local (design TBD)"
    script: |
      local(currentPath = file.getpath());
      return (string.length(currentPath) > 0)
    expected_success: true
    expected_result: "true"

  - name: "file.setpath - set working directory"
    description: "Change working directory and verify"
    skip: true
    skip_reason: "file.getpath/setpath not implemented - CWD should be thread-local (design TBD)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(originalPath = file.getpath());
      local(ok = file.setpath(tmpDir));
      local(newPath = file.getpath());
      file.setpath(originalPath);
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.setpath/getpath - round trip working directory"
    description: "Verify setpath actually changes working directory"
    skip: true
    skip_reason: "file.getpath/setpath not implemented - CWD should be thread-local (design TBD)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(originalPath = file.getpath());
      file.setpath(tmpDir);
      local(newPath = file.getpath());
      file.setpath(originalPath);
      return (newPath == tmpDir)
    expected_success: true
    expected_result: "true"

  # File I/O operations - path-based API
  # NOTE: file.open/read/write/close use PATH-BASED access (not refnums)
  # file.open(path) opens and registers file, returns true/false
  # file.read(path, count) reads from already-opened file at current position
  # file.write(path, data) writes to already-opened file at current position
  # file.close(path) closes file by path

  - name: "file.open/close - open and close file for reading"
    description: "Open file by path, verify success, then close it"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_io.txt");
      file.writeWholeFile(fs, "test content");
      local(ok = file.open(fs));
      file.close(fs);
      return ok
    expected_success: true
    expected_result: "true"

  - name: "file.read - read bytes from file"
    description: "Open file by path and read specific number of bytes"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_read.txt");
      file.writeWholeFile(fs, "Hello World!");
      file.open(fs);
      local(data = file.read(fs, 5));
      file.close(fs);
      return string(data)
    expected_success: true
    expected_result: "Hello"

  - name: "file.read - sequential reads maintain position"
    description: "Multiple reads from same open file advance position"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_read_seq.txt");
      file.writeWholeFile(fs, "abcdefghij");
      file.open(fs);
      local(s1 = file.read(fs, 3));
      local(s2 = file.read(fs, 3));
      file.close(fs);
      return string(s1) + "|" + string(s2)
    expected_success: true
    expected_result: "abc|def"

  - name: "file.read - infinity reads remaining bytes"
    description: "Using infinity as count reads all remaining bytes"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_read_inf.txt");
      file.writeWholeFile(fs, "abcdefghij");
      file.open(fs);
      file.read(fs, 3);
      local(rest = file.read(fs, infinity));
      file.close(fs);
      return (string(rest) == "defghij")
    expected_success: true
    expected_result: "true"

  - name: "file.read - requires file.open first"
    description: "Reading from unopened file should fail"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_read_unopened.txt");
      file.writeWholeFile(fs, "test");
      return file.read(fs, 5)
    expected_success: false
    expected_error: "File not open"

  - name: "file.write - write bytes to open file"
    description: "Write data to already-opened file"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_write_io.txt");
      file.new(fs);
      file.open(fs);
      local(ok = file.write(fs, "hello"));
      file.close(fs);
      local(content = file.readWholeFile(fs));
      return ok and (string(content) == "hello")
    expected_success: true
    expected_result: "true"

  - name: "file.write - requires file.open first"
    description: "Writing to unopened file should fail"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_write_unopened.txt");
      file.new(fs);
      return file.write(fs, "test")
    expected_success: false
    expected_error: "File not open"

  - name: "file.readline - read line from file"
    description: "Read text file line by line"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_readline.txt");
      file.writeWholeFile(fs, "Line 1\nLine 2\nLine 3");
      file.open(fs);
      local(line1 = file.readline(fs));
      file.close(fs);
      return line1
    expected_success: true
    expected_result: "Line 1"

  - name: "file.endoffile - check if at end of file"
    description: "Detect EOF condition after reading past end"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_eof.txt");
      file.writeWholeFile(fs, "AB");
      file.open(fs);
      file.read(fs, 2);
      file.read(fs, 1);
      local(isEof = file.endoffile(fs));
      file.close(fs);
      return isEof
    expected_success: true
    expected_result: "true"

  - name: "file.getposition - get current file position"
    description: "Check file position after reading"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_getpos.txt");
      file.writeWholeFile(fs, "0123456789");
      file.open(fs);
      file.read(fs, 5);
      local(pos = file.getposition(fs));
      file.close(fs);
      return pos
    expected_success: true
    expected_result: "5"

  - name: "file.setposition - seek to file position"
    description: "Move file pointer and read from new position"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_setpos.txt");
      file.writeWholeFile(fs, "ABCDEFGH");
      file.open(fs);
      file.setposition(fs, 3);
      local(data = file.read(fs, 2));
      file.close(fs);
      return string(data)
    expected_success: true
    expected_result: "DE"

  - name: "file.getendoffile - get file size via handle"
    description: "Get file size using open file handle"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(fs = tmpDir + "/" + "frontier_test_geteof.txt");
      file.writeWholeFile(fs, "12345");
      file.open(fs);
      local(size = file.getendoffile(fs));
      file.close(fs);
      return size
    expected_success: true
    expected_result: "5"

  # Folder and file attribute tests
  - name: "file.isfolder - check if path is folder"
    description: "Verify folder detection"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(folderPath = tmpDir + "/" + "test_folder_unique2");
      if file.exists(folderPath) { file.delete(folderPath); };
      file.newfolder(folderPath);
      return file.isfolder(folderPath)
    expected_success: true
    expected_result: "true"

  - name: "file.isfolder - verify file is not folder"
    description: "Check that regular file returns false"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_file.txt");
      file.new(filePath);
      return file.isfolder(filePath)
    expected_success: true
    expected_result: "false"

  - name: "file.compare - compare identical files"
    description: "Compare two files with same content"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(file1 = tmpDir + "/" + "compare1.txt");
      local(file2 = tmpDir + "/" + "compare2.txt");
      file.writeWholeFile(file1, "same content");
      file.writeWholeFile(file2, "same content");
      return file.compare(file1, file2)
    expected_success: true
    expected_result: "true"

  - name: "file.compare - compare different files"
    description: "Compare two files with different content"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(file1 = tmpDir + "/" + "diff1.txt");
      local(file2 = tmpDir + "/" + "diff2.txt");
      file.writeWholeFile(file1, "content A");
      file.writeWholeFile(file2, "content B");
      return file.compare(file1, file2)
    expected_success: true
    expected_result: "false"

  # Edge Cases - Empty file operations
  - name: "file.copy - copy empty file"
    description: "Copy 0-byte file and verify both exist with same size"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(source = tmpDir + "/" + "empty_source.txt");
      local(dest = tmpDir + "/" + "empty_dest.txt");
      file.new(source);
      local(ok = file.copy(source, dest));
      local(sourceSize = file.size(source));
      local(destSize = file.size(dest));
      return ok and (sourceSize == 0) and (destSize == 0)
    expected_success: true
    expected_result: "true"

  - name: "file.move - move empty file"
    description: "Move 0-byte file and verify old gone, new exists"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(source = tmpDir + "/" + "empty_move_source.txt");
      local(dest = tmpDir + "/" + "empty_move_dest.txt");
      file.new(source);
      local(ok = file.move(source, dest));
      return ok and (not file.exists(source)) and file.exists(dest)
    expected_success: true
    expected_result: "true"

  - name: "file.compare - compare two empty files"
    description: "Two 0-byte files should be equal"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(file1 = tmpDir + "/" + "empty1.txt");
      local(file2 = tmpDir + "/" + "empty2.txt");
      file.new(file1);
      file.new(file2);
      return file.compare(file1, file2)
    expected_success: true
    expected_result: "true"

  # Edge Cases - file.setposition beyond EOF
  - name: "file.setposition - position beyond EOF"
    description: "Test seeking beyond end of file"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "setpos_beyond_eof.txt");
      file.writeWholeFile(filePath, "test");
      file.open(filePath);
      local(eofPos = file.getendoffile(filePath));
      local(ok = file.setposition(filePath, eofPos + 100));
      local(newPos = file.getposition(filePath));
      file.close(filePath);
      return ok and (newPos == (eofPos + 100))
    expected_success: true
    expected_result: "true"

  # Edge Cases - file.readline with different line endings
  - name: "file.readline - Unix line ending (LF)"
    description: "Read line with \\n delimiter"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "readline_lf.txt");
      file.writeWholeFile(filePath, "line1\nline2");
      file.open(filePath);
      local(line = file.readline(filePath));
      file.close(filePath);
      return line
    expected_success: true
    expected_result: "line1"

  - name: "file.readline - Windows line ending (CRLF)"
    description: "Read line with \\r\\n delimiter"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "readline_crlf.txt");
      file.writeWholeFile(filePath, "line1\r\nline2");
      file.open(filePath);
      local(line = file.readline(filePath));
      file.close(filePath);
      return line
    expected_success: true
    expected_result: "line1"

  - name: "file.readline - Classic Mac line ending (CR)"
    description: "Read line with \\r delimiter"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "readline_cr.txt");
      file.writeWholeFile(filePath, "line1\rline2");
      file.open(filePath);
      local(line = file.readline(filePath));
      file.close(filePath);
      return line
    expected_success: true
    expected_result: "line1"

  # Edge Cases - file.folderfrompath edge cases
  - name: "file.folderfrompath - filename with no folder"
    description: "Extract folder from bare filename"
    script: |
      local(path = "justfilename.txt");
      local(folder = file.folderfrompath(path));
      return folder
    expected_success: true
    expected_result: ""

  - name: "file.folderfrompath - path with trailing slash"
    description: "Extract folder from path with trailing slash - should return parent with trailing slash"
    script: |
      local(path = "/parent/child/");
      local(folder = file.folderfrompath(path));
      return folder
    expected_success: true
    expected_result: "/parent/"

  # File Type and Creator Tests
  - name: "file.type - extract extension from .txt file"
    description: "Get file extension with dot prefix"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_type.txt");
      file.writeWholeFile(filePath, "test");
      return file.type(filePath)
    expected_success: true
    expected_result: ".txt"

  - name: "file.type - file with no extension"
    description: "Get type of file without extension returns empty string"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "testfile_noext");
      file.writeWholeFile(filePath, "test");
      return file.type(filePath)
    expected_success: true
    expected_result: ""

  - name: "file.type - file with multiple extensions"
    description: "Get type of file.tar.gz returns last extension (space-padded OSType)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "archive.tar.gz");
      file.writeWholeFile(filePath, "test");
      return file.type(filePath)
    expected_success: true
    expected_result: ".gz "

  - name: "file.creator - returns empty string in headless mode"
    description: "Creator codes are Mac-specific, headless returns empty string"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_creator.txt");
      file.writeWholeFile(filePath, "test");
      return file.creator(filePath)
    expected_success: true
    expected_result: ""

  # Timestamp Setter Tests
  - name: "file.setmodified - set modification time"
    description: "Set file modification time and verify it changed"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_setmodified.txt");
      file.writeWholeFile(filePath, "test");
      local(oldTime = file.modified(filePath));
      local(newTime = clock.now() - (60 * 60 * 24));  // 1 day ago
      local(result = file.setmodified(filePath, newTime));
      local(verifyTime = file.modified(filePath));
      return result and (verifyTime < oldTime)
    expected_success: true
    expected_result: "true"

  - name: "file.setcreated - behavior on different platforms"
    description: "Set creation time (works on macOS, returns false on Linux)"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_setcreated.txt");
      file.writeWholeFile(filePath, "test");
      local(newTime = clock.now() - (60 * 60 * 24));  // 1 day ago
      local(result = file.setcreated(filePath, newTime));
      // Returns true on macOS, false on Linux - both are valid
      return typeof(result) == booleanType
    expected_success: true
    expected_result: "true"

  # File Locking Tests - DEFERRED
  # P2: File locking verbs not yet implemented. See issue #324 for implementation plan.
  # These verbs require platform-specific APIs for reliable cross-platform operation.
  - name: "file.islocked - unlocked file returns false"
    description: "Check that writable file is not locked"
    skip: "P2: File locking not yet implemented - deferred pending proper cross-platform API implementation"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_lock.txt");
      file.writeWholeFile(filePath, "test");
      return file.islocked(filePath)
    expected_success: true
    expected_result: "false"

  - name: "file.lock - not implemented on this platform"
    description: "Verify file.lock returns platform error"
    skip: "P2: File locking not yet implemented - deferred pending proper cross-platform API implementation"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_lock2.txt");
      file.writeWholeFile(filePath, "test");
      try {
        file.lock(filePath);
        return "false"  // Should not reach here - lock succeeded unexpectedly
      } else {
        return "true"  // Expected: lock failed with platform error
      }
    expected_success: true
    expected_result: "true"

  - name: "file.unlock - not implemented on this platform"
    description: "Verify file.unlock returns platform error"
    skip: "P2: File locking not yet implemented - deferred pending proper cross-platform API implementation"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "test_lock3.txt");
      file.writeWholeFile(filePath, "test");
      try {
        file.unlock(filePath);
        return "false"  // Should not reach here - unlock succeeded unexpectedly
      } else {
        return "true"  // Expected: unlock failed with platform error
      }
    expected_success: true
    expected_result: "true"

  # Volume Operation Tests
  - name: "file.freespaceonvolume - get free space on root volume"
    description: "Get free space on root filesystem"
    script: |
      local(freeBytes = file.freespaceonvolume("/"));
      return freeBytes > 0
    expected_success: true
    expected_result: "true"

  - name: "file.volumesize - get total size of root volume"
    description: "Get total size of root filesystem"
    script: |
      local(totalBytes = file.volumesize("/"));
      return totalBytes > 0
    expected_success: true
    expected_result: "true"

  - name: "file.volumeblocksize - get block size"
    description: "Get filesystem block size"
    script: |
      local(blockSize = file.volumeblocksize("/"));
      return blockSize > 0
    expected_success: true
    expected_result: "true"

  - name: "file.isvolume - root is always a volume"
    description: "Root path should be detected as a mount point"
    script: |
      return file.isvolume("/")
    expected_success: true
    expected_result: "true"

  - name: "file.isvolume - regular directory is not a volume"
    description: "Subdirectory should not be detected as mount point"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      return !file.isvolume(tmpDir)
    expected_success: true
    expected_result: "true"

  # File Dialog Verbs - Phase 1: Should Error in Headless Mode
  # These tests validate that file dialog verbs properly detect headless/batch mode
  # and return unimplementedverberror instead of hanging or crashing.
  #
  # TODO: When Phase 2 (stdio prompts) is implemented, update these tests to:
  # - Test interactive mode with stdin provided
  # - Test batch mode (--batch flag) still errors
  # - Test CI environment (CI=true) forces batch mode
  #
  # Reference: planning/phase3/HEADLESS_INTERACTIVE_MODE.md

  - name: "file.getFileDialog - returns false in headless batch mode"
    description: "File picker dialog should return false (cancelled) in batch/headless mode"
    script: |
      local(selectedFile);
      local(result = file.getFileDialog("Select a file", @selectedFile));
      return result
    expected_success: true
    expected_result: "false"

  - name: "file.putFileDialog - returns false in headless batch mode"
    description: "Save file dialog should return false (cancelled) in batch/headless mode"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(defaultPath = tmpDir + "/" + "save_target.txt");
      local(selectedFile);
      local(result = file.putFileDialog("Save file as", defaultPath, @selectedFile));
      return result
    expected_success: true
    expected_result: "false"

  - name: "file.getFolderDialog - returns false in headless batch mode"
    description: "Folder picker dialog should return false (cancelled) in batch/headless mode"
    script: |
      local(selectedFolder);
      local(result = file.getFolderDialog("Select a folder", @selectedFolder));
      return result
    expected_success: true
    expected_result: "false"

  - name: "file.getDiskDialog - returns false in headless batch mode"
    description: "Volume/disk picker dialog should return false (cancelled) in batch/headless mode"
    script: |
      local(selectedDisk);
      local(result = file.getDiskDialog("Select a volume", @selectedDisk));
      return result
    expected_success: true
    expected_result: "false"

  # file.folderFromPath tests - includes trailing separator per Mac behavior
  - name: "file.folderFromPath - includes trailing separator"
    description: "folderFromPath should include trailing / per Mac behavior"
    script: |
      return file.folderFromPath("/Users/test/file.txt")
    expected_success: true
    expected_result: "/Users/test/"

  - name: "file.folderFromPath - trailing separator on directory path"
    description: "folderFromPath on path ending with / returns parent with /"
    script: |
      return file.folderFromPath("/Users/test/subdir/")
    expected_success: true
    expected_result: "/Users/test/"

  - name: "file.folderFromPath - root path"
    description: "folderFromPath on root returns root"
    script: |
      return file.folderFromPath("/")
    expected_success: true
    expected_result: "/"

  - name: "file.folderFromPath - simple filename"
    description: "folderFromPath on filename with no path returns empty"
    script: |
      return file.folderFromPath("file.txt")
    expected_success: true
    expected_result: ""

  # file.isFolder tests
  - name: "file.isFolder - existing directory returns true"
    description: "isFolder on existing directory returns true"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      return file.isFolder(tmpDir)
    expected_success: true
    expected_result: "true"

  - name: "file.isFolder - existing file returns false"
    description: "isFolder on existing file returns false"
    script: |
      local(tmpDir = "{FRONTIER_TEST_TMP_DIR}");
      local(filePath = tmpDir + "/" + "isFolder_test.txt");
      file.new(filePath);
      return file.isFolder(filePath)
    expected_success: true
    expected_result: "false"

  - name: "file.isFolder - nonexistent path errors"
    description: "isFolder on nonexistent path should error (not return false)"
    script: |
      return file.isFolder("/nonexistent/path/12345")
    expected_success: false
    expected_error: "Can't find"

  # Note: Test runner automatically removes {FRONTIER_TEST_TMP_DIR} after all tests
  # No explicit cleanup needed - all test files are automatically removed
